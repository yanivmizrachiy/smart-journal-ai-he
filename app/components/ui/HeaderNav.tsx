import { Link,useNavigate,useLocation } from 'react-router-dom'
function getIsraelYM(){const f=new Intl.DateTimeFormat('he-IL',{timeZone:'Asia/Jerusalem',year:'numeric',month:'2-digit'});const p=Object.fromEntries(f.formatToParts(new Date()).map(x=>[x.type,x.value]));return `${p.year}-${p.month}`}
export default function HeaderNav(){const nav=useNavigate();const {pathname}=useLocation();const is=(p:string)=>pathname.startsWith(p);return(<header style={{position:'sticky',top:0,background:'#fff',borderBottom:'1px solid #e5e7eb',padding:'10px 12px',zIndex:40}}><div style={{display:'flex',alignItems:'center',gap:'8px'}}><strong style={{fontSize:'20px'}}>היומן החכם</strong><nav style={{marginInlineStart:'auto',display:'flex',gap:'6px'}}><Link className={is('/month')?'btn-primary':'btn'} to={`/month/${getIsraelYM()}`}>חודש</Link><Link className={is('/week')?'btn-primary':'btn'} to="/week/2025-W43">שבוע</Link><Link className={is('/day')?'btn-primary':'btn'} to="/day/2025-10-21">יום</Link><Link className={is('/settings')?'btn-primary':'btn'} to="/settings">הגדרות</Link></nav><button className="btn" onClick={()=>nav(`/month/${getIsraelYM()}`)}>היום</button></div></header>)}
